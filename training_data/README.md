# training_data

Contrato reservado para una futura fase de fine-tuning:

```text
training_data/
  <speaker_id>/
    raw/
    processed/
    metadata.json
```

La v1 no usa esta carpeta todavia. Se deja creada para evitar cambiar la UI cuando se anada entrenamiento en una fase posterior.

